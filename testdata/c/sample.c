// greet function
char* greet(char* name) {
    return name;
}

// add function
int add(int a, int b) {
    return a + b;
}

// Point struct
struct Point {
    double x;
    double y;
};

// Color enum
enum Color { RED, GREEN, BLUE };
