// Greeter class
class Greeter {

    // greet method
    public String greet(String name) {
        return "Hello, " + name + "!";
    }

    // add method
    public int add(int a, int b) {
        return a + b;
    }
}

// Shape interface
interface Shape {

    // area method
    int area();
}

// Color enum
enum Color {
    RED, GREEN, BLUE
}
